import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sales & Support | UV Curing Equipment Service | ETIA",
  description:
    "Get expert UV curing system selection, application consulting, repair, and calibration support. Offices in China, Hong Kong, Thailand & Vietnam.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
